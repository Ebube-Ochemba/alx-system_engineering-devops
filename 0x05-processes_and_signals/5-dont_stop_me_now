#!/usr/bin/env bash
# A Bash script that stops 4-to_infinity_and_beyond process. Requirements: You must use 'kill' command.
# shellcheck disable=SC2009

pid=$(pgrep -f 4-to_infinity_and_beyond)
kill "$pid"
